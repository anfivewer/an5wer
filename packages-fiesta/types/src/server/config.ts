export type Config = {
  isDev: boolean;
  isDebug: boolean;
  dataPath: string;
  adminPath: string;
  serverPort: number;
  directusPath: string;
  directusPort: number;
  buildsPath: string;
  directusAdminEmail: string;
  directusAdminPassword: string;
};
